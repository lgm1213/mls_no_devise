<% provide(:title, 'Edit Account Info') %>
<% provide(:button_text, 'Update User Account') %>

<h1>Editing User</h1>
<div class="ibox-content">
	<%= render 'form', user: @user %>

	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>
	
</div>
